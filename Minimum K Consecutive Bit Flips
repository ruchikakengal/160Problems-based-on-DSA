class Solution {
    public int kBitFlips(int[] arr, int k) {
        int n = arr.length;
        int flips = 0, res = 0;
        int[] isFlipped = new int[n];
        
        for (int i = 0; i < n; i++) {
            if (i >= k) flips ^= isFlipped[i - k];
            
            if ((arr[i] ^ flips) == 0) {
                if (i + k > n) return -1;
                res++;
                flips ^= 1;
                isFlipped[i] = 1;
            }
        }
        return res;
    }
}
